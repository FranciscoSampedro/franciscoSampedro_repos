import * as componentHook from "@hooks/usePokemonPagination";
import { IUsePokemonPagination, usePokemonPagination } from "@hooks/usePokemonPagination";

import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";


describe("Home Component", (): void => {
    
   
    // let wrapper: Render;

    // const TestComponent: React.FC = () => {
    //     let props = usePokemonPagination();
    //     // @ts-ignore
    //     return <div { ...props } />;
    // };
    
    // afterEach(() => {
    //     wrapper.unmount();
    // });


    // it("deberia ser funcion", (): void => {
    //     render(<TestComponent />)
    // })
}
)